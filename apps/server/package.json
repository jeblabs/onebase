{
  "name": "server",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "hono": "^4.8.2",
    "@hono/node-server": "^1.14.4",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "zod": "^4.1.11",
    "@onebase/auth": "workspace:*",
    "@onebase/db": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.2",
    "tsx": "^4.19.2",
    "@types/node": "^22.13.11",
    "tsdown": "^0.16.5",
    "@onebase/config": "workspace:*"
  }
}
